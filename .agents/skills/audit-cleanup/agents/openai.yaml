interface:
  display_name: "Audit Cleanup"
  short_description: "Execute approved audit cleanup"
  default_prompt: "Use $audit-cleanup to execute an approved Markdown cleanup plan through tracker tasks and phase guards."
