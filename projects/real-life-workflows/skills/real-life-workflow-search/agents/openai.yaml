interface:
  display_name: "Real-Life Workflow Search"
  short_description: "Search workflows and write packets"
  default_prompt: "Use $real-life-workflow-search to create a workflow-understanding packet for the operator job I describe."
policy:
  allow_implicit_invocation: true
