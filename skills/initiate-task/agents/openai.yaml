interface:
  display_name: "Initiate Task"
  short_description: "Start or resume JSON-tracked workflow tasks"
  default_prompt: "Use $initiate-task to start or resume a project workflow task with explicit Matt phase control."
