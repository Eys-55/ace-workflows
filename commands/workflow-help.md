# /workflow-help

Use the canonical skill at `skills/workflow-help/SKILL.md`.

This command is a compatibility shim only. Do not add independent workflow
logic here.

Forward the user's request unchanged to `$workflow-help`, for example:

```text
/workflow-help
/workflow-help what can I do here?
```
